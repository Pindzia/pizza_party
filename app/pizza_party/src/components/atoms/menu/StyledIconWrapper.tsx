import { Box } from "@mui/material";
import styled from "@mui/material/styles/styled";

const StyledIconWrapper = styled(Box)({
  "&&&": { marginLeft: "0.7rem", backgroundColor: "transparent" },
});

export default StyledIconWrapper;
